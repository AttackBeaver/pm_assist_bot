# PM‑Assist Bot

**AI‑ассистент project‑менеджера для автоматизации задач из чатов и встреч**  

## Ссылка доступа: <https://t.me/pm_assists_bot> (отключен)

Бот читает текстовые и голосовые сообщения в Telegram‑чатах, подключается к созвонам (Яндекс.Телемост), распознаёт задачи, дедлайны и ответственных (включая нескольких), автоматически создаёт карточки в **YouGile**, управляет статусами, присылает уведомления и напоминания, а также предоставляет веб‑кабинет с геймификацией (XP, уровни, ачивки).

- Бот развёрнут на **Timeweb VPS (Ubuntu 24.04)** и работает в production-режиме.
- **Автоматическое подключение к Яндекс Телемосту через Playwright + MediaRecorder** полностью реализовано и успешно протестировано.

> Разработано командой **Codлета** в рамках хакатона **Цифровой прорыв 2026**

## Возможности

### Автоматическое распознавание задач

| Тип | Технология |
| ----- | ------------ |
| **Текст** | Гибридный парсер: YandexGPT (основной) + regex (fallback) |
| **Голосовые сообщения** | Транскрибация через Speech2Text.ru |
| **Аудиофайлы** | MP3, WAV, OGG, AAC, WMA |
| **Видеофайлы** | WEBM, MP4, AVI, MOV, MKV (извлечение аудио через ffmpeg) |
| **Ссылки на Яндекс.Диск** | Публичные ссылки на аудио/видео файлы |
| **Несколько ответственных** | Отдельная карточка для каждого @username |
| **Дедлайны** | Естественно‑языковые выражения (сегодня, завтра, до пятницы, через 2 дня, 15 июля и др.) |

### Управление задачами в YouGile

- **Автоматическое создание** карточек в колонке «Сделать» (с дедлайном и описанием).
- **Перемещение** по колонкам: «В процессе», «Готово» (через кнопки или команды).
- **Удаление** (только автор задачи может удалить карточку).
- **Полная синхронизация** статусов с локальной БД (SQLite).

### Проактивные напоминания

- **За 2 часа до дедлайна** – личное сообщение ответственному + сообщение в групповой чат.
- **Stale‑напоминания** – раз в 6 часов о задачах, зависших более 3 дней.
- **Вечерний дайджест** – каждый день в 19:00 список активных задач каждому пользователю.

### Гибридный парсер задач

1. **Основной** – YandexGPT (облачная LLM) – высокая точность, понимает контекст.
2. **Fallback** – regex‑парсер с ключевыми словами и стоп‑словами – работает офлайн, точность >80% на тестовых фразах.
3. **Доверительный скор** – решение принимается только при confidence >= 85%.

### Обработка встреч и записей (три способа)

#### Способ 1. Загрузка аудио/видео файла или ссылки на Яндекс.Диск

- Работает на любом хостинге.
- Бот скачивает → транскрибирует через Speech2Text.ru → извлекает задачи → создаёт карточки в YouGile.
- Поддерживаются файлы до **20 МБ** (Telegram) и до **200 МБ** через Яндекс.Диск.

#### Способ 2. Автоматическое подключение через mymeet.ai

- Требуется корпоративный API‑ключ mymeet.ai (для юридических лиц).
- Пользователь отправляет ссылку на встречу → бот вызывает API mymeet.ai → сервис подключается, записывает, транскрибирует → бот получает результат и создаёт задачи.
- Поддерживаются: Яндекс Телемост, Zoom, Google Meet, Microsoft Teams, TrueConf, Jitsi и др.

#### Способ 3. Автономное подключение через Playwright + MediaRecorder (основной, полностью рабочий)

- **Работает на выделенном сервере (Timeweb VPS)** без необходимости в PulseAudio loopback.
- Бот открывает браузер (Chromium) через Xvfb (виртуальный дисплей), подключается к встрече, **захватывает аудиопоток всех участников** с помощью JavaScript MediaRecorder, сохраняет файл WebM, конвертирует в WAV (16 кГц, моно), транскрибирует через Speech2Text.ru, генерирует саммари встречи через YandexGPT и создаёт задачи в YouGile.
- Этот метод является **реальным конкурентом mymeet.ai** и полностью реализован в проекте.

> **На демонстрационном сервере (Timeweb)** доступны все три способа. Способ 3 используется как основной для автоматической записи встреч.

### Веб‑кабинет (FastAPI)

- Полноценный веб‑кабинет на **FastAPI** работает на VPS (Timeweb).
- Доступен по адресу: `http://<IP-сервера>:8000/cabinet/<telegram_id>`.
- Функционал:
  - Таблица всех задач (с фильтрацией по статусу).
  - Кнопки «Выполнить», «Удалить», «Взять в работу».
  - Статистика: XP, уровень, количество задач, среднее время выполнения.
  - Аналитика эффективности: % выполненных в срок, среднее время в работе.
  - Ачивки с иконками.
  - Рекомендации по курсам на основе выполненных задач.
- Альтернативный кабинет на **Streamlit** также доступен для обратной совместимости.

### Геймификация

- **XP** за создание задачи: +5 XP (автору).
- **XP** за выполнение задачи: +10 XP (ответственному).
- **Уровни**: 1 уровень = 100 XP.
- **Ачивки**:
  - Первая задача – создана первая задача.
  - Спринтер – выполнено 3 задачи.
  - Мастер – достигнут 2 уровень (200 XP).

### Команды и кнопки

| Команда / кнопка | Описание |
| ------------------ | ---------- |
| `/start` | Регистрация, главная клавиатура |
| `/help` | Справка |
| `📋 Мои задачи` | Интерактивный список активных задач |
| `🌐 Личный кабинет` | Статистика, достижения, рекомендации, ссылка на веб‑кабинет |
| `/stats` | Статистика (XP, уровень, задачи) |
| `/achievements` | Достижения |
| `/deadlines` | Ближайшие дедлайны |
| `/recommendations` | Рекомендации по курсам |
| `/move <номер> <колонка>` | Переместить задачу |
| `/complete <номер>` | Завершить задачу |
| `/away [причина]` | Отметка недоступным (7 дней) |
| `/back` | Вернуться в работу |
| `📞 Встреча` | Меню выбора способа обработки встречи |
| `👨‍💼 Тест менеджера` | Автоматический сценарий менеджера |
| `👷 Тест исполнителя` | Автоматический сценарий исполнителя |

## Тестовые сценарии

- **Тест менеджера** – создание задачи → перемещение в «В процессе» → завершение → отображение XP и ачивок.
- **Тест исполнителя** – получение задачи от менеджера → взятие в работу → завершение → начисление XP.
- **Автоматический прогон** всех этапов без ручного ввода сообщений.

## Технологический стек

| Компонент | Технология |
| ----------- | ------------ |
| Язык | Python 3.11 |
| Telegram Bot | aiogram 3.x |
| Веб‑кабинет | FastAPI + uvicorn |
| База данных | SQLite |
| Распознавание речи | Speech2Text.ru API |
| LLM | YandexGPT (REST API) |
| Канбан‑доска | YouGile API v2 |
| Конфигурация | python-dotenv |
| Хостинг | Timeweb VPS (Ubuntu 24.04) |
| Автоматизация браузера | Playwright + Chromium |
| Виртуальный дисплей | Xvfb |
| Дополнительно | ffmpeg |

## Структура проекта

```txt
pm_assist_bot/
├── main.py                        # Точка входа (бот + веб‑сервер конкурентно)
├── config.py                      # Загрузка переменных окружения
├── requirements.txt
├── speech2text_client.py          # Клиент Speech2Text.ru
├── yougile_client.py              # Клиент YouGile API
├── streamlit_app.py               # Альтернативный веб‑кабинет (Streamlit)
├── bot/
│   ├── handlers/
│   │   ├── user_commands.py       # Команды и главная клавиатура
│   │   ├── message_handler.py     # Текстовые сообщения (группы)
│   │   ├── voice_handler.py       # Голос, аудио, видео, ссылки Яндекс.Диск
│   │   └── callbacks.py           # Inline‑кнопки
│   ├── tasks/
│   │   └── scheduler.py           # Планировщик напоминаний
│   └── utils/
│       ├── parser.py              # Regex‑парсер (fallback)
│       ├── llm_parser.py          # YandexGPT‑парсер
│       ├── date_utils.py          # Преобразование дедлайнов
│       ├── audio_utils.py         # Транскрибация, конвертация
│       ├── link_utils.py          # Яндекс.Диск
│       ├── meet_utils.py          # Обработка ссылок на встречи
│       ├── mymeet_client.py       # Клиент mymeet.ai (опционально)
│       ├── meet_automation.py     # Playwright + MediaRecorder (основной)
│       └── yougile_utils.py       # Обёртка создания задачи
├── web/
│   ├── app.py                     # FastAPI (личный кабинет)
│   └── database.py                # SQLite (users, tasks, stats, history)
└── qa/                            # Тесты (test_full.py, test_yandex.py)
```

## Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone <url>
cd pm_assist_bot
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
python -m venv venv
source venv/bin/activate   # Linux/Mac
venv\Scripts\activate      # Windows
pip install -r requirements.txt
```

### 3. Создать файл `.env` (пример)

```env
# Telegram
BOT_TOKEN=your_telegram_bot_token

# Распознавание речи
SPEECH2TEXT_API_KEY=your_speech2text_api_key

# YouGile
YOUGILE_TOKEN=your_yougile_bearer_token
YOUGILE_BOARD_ID=your_board_id
YOUGILE_TO_COLUMN_ID=id_колонки_Сделать
YOUGILE_DO_COLUMN_ID=id_колонки_В_процессе
YOUGILE_DONE_COLUMN_ID=id_колонки_Готово

# YandexGPT (опционально, но повышает точность)
YANDEX_FOLDER_ID=your_yandex_cloud_folder_id
YANDEX_API_KEY=your_api_key

# mymeet.ai (опционально)
MYMEET_API_KEY=your_api_key
MYMEET_API_URL=https://api.mymeet.ai/v1

# Веб‑кабинет
WEB_BASE_URL=http://localhost:8000   # замените на IP или домен сервера
PORT=8000
```

### 4. Запуск бота

```bash
python main.py
```

Веб‑кабинет будет доступен по адресу `http://localhost:8000` (локально) или по внешнему IP сервера.

## Docker (опционально)

Проект может быть упакован в Docker-образ. Для работы автоматического подключения к встречам через Playwright необходимо пробросить X11-сокет и настроить звук. Рекомендуется использовать прямое развёртывание на VPS для максимальной совместимости.

## Тестирование

### Полное интеграционное тестирование

```bash
python qa/test_full.py
```

Проверяет: переменные окружения, парсер (≥80%), БД и геймификацию, интеграцию с YouGile, веб‑кабинет, планировщик, уведомления, YandexGPT (если настроен), Speech2Text (если есть тестовый аудиофайл).

## Примеры работы

### Текстовое сообщение в группе

```txt
@attack_beaver нужно сделать отчёт по продажам до 15 июля
```

→ Бот создаёт карточку в YouGile, @attack_beaver получает личное сообщение с кнопками управления. В чат отправляется информационное сообщение.

### Совместная задача

```txt
@ivan и @max проведите рефакторинг проекта, срок – две недели
```

→ Создаются две карточки (каждому ответственному), каждый получает уведомление.

### Обработка записи встречи

- Отправьте боту файл `.webm`, `.mp4` или аудиофайл.
- Или отправьте **публичную ссылку на Яндекс.Диск** с записью.
- Бот распознаёт речь, выделяет задачи, создаёт карточки в YouGile.

### Автоматическое подключение к встрече (основной способ)

Просто отправьте боту ссылку на Яндекс Телемост в любом чате, например:

```txt
Привет, встреча через 10 минут: https://telemost.yandex.ru/j/123456789
```

Бот автоматически:

- Подключится к встрече (через Playwright + Xvfb).
- Запишет аудио всех участников.
- Распознает речь, сделает саммари.
- Создаст карточки задач в YouGile.
- Отправит аудиозапись и саммари в чат.

## Команда

| Роль | Имя | Telegram | Email |
| ------ | ----- | ---------- | ------- |
| Team Lead | Александр | @attack_beaver | <astarikov820@gmail.com> |
| Backend Developer | Дарья | @Aoaoaaoaoaoaoaoa777 | <dara.ponomarewa@gmail.com> |
| NLP Engineer | Егор | @F3NR1R55 | <maik0337@gmail.com> |
| Integration Specialist | Антон | @Anton_Belyaninov | <Belyaninov.anton@gmail.com> |
| QA & Frontend Assistant | Вероника | @m_u_shro_o_m | <weronika_2007@inbox.ru> |

## Лицензия

[MIT](LICENSE)
